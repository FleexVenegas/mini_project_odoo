<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
    ============================================================
    ACTIONS FILTERED BY ACTIVE INSTANCE
    ============================================================
    Each server action here reads the user's active instance
    (env.user.woo_active_instance_id) and filters the
    corresponding model. If no instance is selected, it shows
    a warning asking to select one.

    To add a new filtered menu:
      1. Copy one of the blocks below
      2. Change the id, name, res_model and domain
      3. Add the menuitem in woo_menu.xml pointing to the new id
    ============================================================
    -->

    <!-- ── ORDERS ──────────────────────────────────────────────── -->
    <record id="action_server_orders_by_instance" model="ir.actions.server">
        <field name="name">Orders by Instance</field>
        <field name="model_id" ref="model_woo_instance"/>
        <field name="state">code</field>
        <field name="code">
instance = env.user.woo_active_instance_id
if not instance:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {
            'title': 'No instance selected',
            'message': 'Please select an instance from the Instances menu.',
            'type': 'warning',
            'sticky': False,
        },
    }
else:
    action = {
        'type': 'ir.actions.act_window',
        'name': 'Orders - %s' % instance.name,
        'res_model': 'odoo.wp.sync',
        'view_mode': 'tree,form',
        'domain': [('instance_id', '=', instance.id)],
        'context': {'default_instance_id': instance.id},
    }
        </field>
    </record>

    <!-- ── WOOCOMMERCE PRODUCTS ────────────────────────────────────────── -->
    <record id="action_server_products_by_instance" model="ir.actions.server">
        <field name="name">Products by Instance</field>
        <field name="model_id" ref="model_woo_instance"/>
        <field name="state">code</field>
        <field name="code">
instance = env.user.woo_active_instance_id
if not instance:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {
            'title': 'No instance selected',
            'message': 'Please select an instance from the Instances menu.',
            'type': 'warning',
            'sticky': False,
        },
    }
else:
    action = {
        'type': 'ir.actions.act_window',
        'name': 'Products - %s' % instance.name,
        'res_model': 'woo.product',
        'view_mode': 'tree,form',
        'domain': [('instance_id', '=', instance.id)],
        'context': {'default_instance_id': instance.id},
    }
        </field>
    </record>

    <!--
    ── PLANTILLA PARA NUEVAS SECCIONES ────────────────────────
    <record id="action_server_NOMBRE_by_instance" model="ir.actions.server">
        <field name="name">NOMBRE por Instancia</field>
        <field name="model_id" ref="model_woo_instance"/>
        <field name="state">code</field>
        <field name="code">
instance = env.user.woo_active_instance_id
if not instance:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {
            'title': 'No instance selected',
            'message': 'Please select an instance from the Instances menu.',
            'type': 'warning',
            'sticky': False,
        },
    }
else:
    action = {
        'type': 'ir.actions.act_window',
        'name': 'NAME - %s' % instance.name,
        'res_model': 'THE_MODEL_HERE',
        'view_mode': 'tree,form',
        'domain': [('instance_id', '=', instance.id)],
        'context': {'default_instance_id': instance.id},
    }
        </field>
    </record>
    -->

</odoo>
