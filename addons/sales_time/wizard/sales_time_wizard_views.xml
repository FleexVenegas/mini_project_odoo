<odoo>
    <data>
        <!-- Vista Form del Wizard -->
        <record id="view_sales_time_wizard_form" model="ir.ui.view">
            <field name="name">sales.time.wizard.form</field>
            <field name="model">sales.time.wizard</field>
            <field name="arch" type="xml">
                <form string="Reporte de Tiempos de Entrega">
                    <group>
                        <group>
                            <field name="order_ids" widget="many2many_tags" readonly="1" string="Órdenes Seleccionadas"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Reporte" invisible="not report_html">
                            <field name="report_html" nolabel="1" widget="html"/>
                        </page>
                    </notebook>
                    <footer>
                        <button name="action_generate_report" string="🔄 Generar Reporte" type="object" class="btn-primary" icon="fa-refresh"/>
                        <button string="Cerrar" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>
